% Fig 3.4 — mDPR encoding flow
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[node distance=10mm]
  \node[thQuery, minimum width=48mm] (q) {\thIconUser~Query batch (64)};
  \node[thRet, minimum width=48mm, below=of q] (enc) {\thIconLLM~mDPR encoder\\\texttt{castorini/mdpr-tied-pft-msmarco}};
  \node[thRet, minimum width=48mm, below=of enc] (gpu) {GPU batch encoding\\(T4 / A100, FP16)};
  \node[thData, minimum width=44mm, below=of gpu] (vec) {Query vectors\\($768$-dim)};
  \node[thData, minimum width=44mm, right=12mm of vec] (idx) {\thIconCog~FAISS index\\(pre-built, 5.47\,GB)};
  \node[thRet, minimum width=48mm, below=of vec] (search) {Top-100 inner-product\\search};
  \node[thOut, minimum width=48mm, below=of search] (out) {Ranked passages};

  \draw[thArrow] (q) -- (enc);
  \draw[thArrow] (enc) -- (gpu);
  \draw[thArrow] (gpu) -- (vec);
  \draw[thArrow] (vec) -- (search);
  \draw[thArrow] (idx) |- (search);
  \draw[thArrow] (search) -- (out);
\end{tikzpicture}
\end{document}
