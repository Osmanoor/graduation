% Fig 3.5 — Query2Doc generation pipeline
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[
    node distance=10mm and 14mm,
    prompt/.style={rectangle, draw=thLLMS, dashed, rounded corners=2pt,
                   minimum width=58mm, minimum height=24mm,
                   align=left, font=\footnotesize, fill=thLLMF,
                   inner sep=4pt}
]
  \node[thQuery] (q) {\thIconUser~User query (short)};

  \node[prompt, right=of q, xshift=4mm] (tmpl)
    {\textbf{System:} ``You are asked to write a passage\\that answers the given query. Respond\\in Arabic only.''\\
     \textbf{User:} \texttt{<query>}};

  \node[thLLM, minimum width=36mm, below=of tmpl] (llm) {\thIconLLM~Aya 8B / Jais-2 / \dots\\(zero-shot, FP16/4-bit)};

  \node[thData, below=of llm] (pdoc) {\thIconOut~Pseudo-document\\($\sim$128 tokens)};

  \node[thFusion, minimum width=34mm, left=of pdoc, xshift=-6mm] (concat) {\thIconFusion~Concatenate\\$q \oplus \mathrm{pdoc}$};

  \node[thRet, minimum width=34mm, below=of concat] (retr) {\thIconRet~Pass to retriever};

  \draw[thArrow] (q.east) -- (tmpl.west);
  \draw[thArrow] (tmpl) -- (llm);
  \draw[thArrow] (llm) -- (pdoc);
  \draw[thArrow] (pdoc) -- (concat);
  \draw[thArrow] (q.south) -- ++(0, -38mm) -| (concat.north);
  \draw[thArrow] (concat) -- (retr);

  \node[thLabel, right=2mm of llm.east, align=left]
    {max\_new\_tokens=128\\temperature=0.1--0.7\\top\_p=0.9};
\end{tikzpicture}
\end{document}
