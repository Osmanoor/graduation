% Fig 3.1 — End-to-end thesis pipeline
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[node distance=14mm and 12mm]
  \node[thQuery] (q) {\thIconUser~User query};
  \node[thLLM, right=of q] (qe) {\thIconLLM~Query\\enhancement};
  \node[thRet, right=of qe] (r1) {\thIconCog~BM25S};
  \node[thRet, below=6mm of r1] (r2) {\thIconRet~mDPR};
  \node[thFusion, right=of r1, yshift=-6mm] (fus) {\thIconFusion~Fusion\\(CC / RRF)};
  \node[thOut, right=of fus] (topk) {Top-$k$\\passages};
  \node[thBase, fill=thOutF, draw=thOutS, line width=0.8pt, below=of topk] (eval) {\thIconEval~Evaluation\\NDCG@10 $\cdot$ R@10 $\cdot$ MRR};

  \draw[thArrow] (q) -- (qe);
  \draw[thArrow] (qe.east) -- ++(8mm,0) |- (r1.west);
  \draw[thArrow] (qe.east) -- ++(8mm,0) |- (r2.west);
  \draw[thArrow] (r1.east) -| (fus.north);
  \draw[thArrow] (r2.east) -| (fus.south);
  \draw[thArrow] (fus) -- (topk);
  \draw[thArrow] (topk) -- (eval);

  \node[thLabel, above=2mm of qe] {LLM-generated pseudo-doc};
  \node[thLabel, below=2mm of eval] {MIRACL Arabic dev qrels};
\end{tikzpicture}
\end{document}
