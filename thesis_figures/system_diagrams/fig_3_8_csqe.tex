% Fig 3.8 — CSQE (Corpus-Steered Query2Doc) pipeline
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[node distance=10mm and 14mm]
  \node[thQuery] (q) {\thIconUser~User query};

  % Stage 1
  \node[thRet, right=of q] (bm1) {\thIconCog~BM25\\first-pass};
  \node[thData, right=of bm1] (k5) {Top-$k_1$\\($k_1{=}5$)};

  % Stage 2
  \node[thFusion, below=of bm1] (extract) {Extract corpus\\context};
  \node[thLLM, right=of extract] (gen) {\thIconLLM~Aya LLM\\generate};
  \node[thData, right=of gen] (pdoc) {\thIconOut~Corpus-grounded\\pseudo-doc};

  % Stage 3
  \node[thFusion, below=of extract] (concat) {Concat: $q^{\alpha} \oplus$ pdoc};
  \node[thRet, right=of concat] (bm2) {\thIconCog~BM25\\second-pass};
  \node[thOut, right=of bm2] (final) {\thIconStar~Final top-100};

  \draw[thArrow] (q) -- (bm1);
  \draw[thArrow] (bm1) -- (k5);
  \draw[thArrow] (k5.south) |- ($(extract.east) + (5mm,2mm)$);
  \draw[thArrow] (q.south) |- (extract.west);
  \draw[thArrow] (extract) -- (gen);
  \draw[thArrow] (gen) -- (pdoc);
  \draw[thArrow] (pdoc.south) |- (concat.east);
  \draw[thArrow] (q.south) |- (concat.west);
  \draw[thArrow] (concat) -- (bm2);
  \draw[thArrow] (bm2) -- (final);

  \begin{scope}[on background layer]
    \node[thStage, above=1mm of bm1.north east] {Stage 1};
    \node[thStage, above=1mm of gen.north] {Stage 2};
    \node[thStage, above=1mm of bm2.north] {Stage 3};
  \end{scope}
\end{tikzpicture}
\end{document}
