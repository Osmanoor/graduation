% Fig 3.9 — Best system: BM25-only-expanded RRF k=20
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[node distance=10mm and 12mm]
  \node[thQuery] (q) {\thIconUser~User query};

  % Top: BM25 with CSQE
  \node[thHi, above right=2mm and of q] (csqeq) {\thIconStar~Query + CSQE\\corpus-grounded pdoc};
  \node[thHi, right=of csqeq] (bm) {\thIconCog~BM25 retrieval};

  % Bottom: mDPR with original
  \node[thQuery, below right=2mm and of q] (origq) {Original short query};
  \node[thRet, right=of origq] (dn) {\thIconRet~mDPR retrieval};

  % Fusion
  \node[thFusion, minimum width=32mm, right=14mm of $(bm.east)!0.5!(dn.east)$] (rrf) {\thIconFusion~RRF fusion\\$k = 20$};
  \node[thOut, right=of rrf] (top) {\thIconStar~Top-100\\NDCG@10 = 0.714};

  \draw[thArrow] (q) -| (csqeq);
  \draw[thArrow] (q) -| (origq);
  \draw[thArrow] (csqeq) -- (bm);
  \draw[thArrow] (origq) -- (dn);
  \draw[thArrow] (bm.east) -| (rrf.north);
  \draw[thArrow] (dn.east) -| (rrf.south);
  \draw[thArrow] (rrf) -- (top);
\end{tikzpicture}
\end{document}
