% Fig 2.1 — Retrieval-Augmented Generation (RAG) architecture
\documentclass[border=4pt]{standalone}
\usepackage{tikz}
\input{_style.tex}

\begin{document}
\begin{tikzpicture}[node distance=12mm and 14mm]
  \node[thQuery] (q) {\thIconUser~User query};
  \node[thRet, right=of q] (retriever) {\thIconRet~Retriever};
  \node[thLLM, right=of retriever] (llm) {\thIconLLM~LLM};
  \node[thOut, right=of llm] (ans) {\thIconOut~Generated\\answer};
  \node[thData, above=of retriever] (corpus) {\thIconCorpus~Corpus};

  \draw[thArrow] (q) -- (retriever);
  \draw[thArrow] (retriever) -- (llm) node[thLabel, midway, below=1mm] {top-$k$};
  \draw[thArrow] (llm) -- (ans);
  \draw[thArrow] (corpus) -- (retriever);

  \node[thLabel, above=1mm of llm] {context-augmented prompt};
\end{tikzpicture}
\end{document}
